#include <iostream>
#include "da.h"

using namespace std;

int main(int argc, char const *argv[])
{
    DArray arr;
    arr.insert(10, true);
    arr.insert(0, true);
    arr.insert(1, true);
    arr.insert(4, true);
    return 0;
}
